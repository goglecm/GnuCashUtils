# Test Plan & Strategy (v1)

## Status: Implemented — see `pytest` for current count

**How to add tests and when to update this document**: see `docs/developers_guide.mdc` (§ How to Add Tests, § Keeping Documentation and Spec Up to Date). After adding tests or a new test file, update the Test Matrix and Spec coverage below so they stay accurate.

## Objectives
- Provide immediate, repeatable confidence that all modules work correctly.
- Cover happy paths, edge cases, and failure paths for each module.
- End-to-end integration tests verify the full run -> review -> apply cycle.

## Testing Levels

1. **Unit tests**
   - Pure function/service logic with real fixtures and constructed data.
   - Matching score logic, filters, state transitions.
   - Config and domain model construction and defaults.

2. **Adapter tests**
   - GnuCash XML read/write with synthetic gzip-compressed fixture.
   - EML parser with real-format fixture `.eml` files.
   - Receipt OCR with programmatically generated images (via Pillow).
   - State repository JSON/JSONL roundtrip tests.

3. **Integration tests**
   - End-to-end `run -> review decision -> apply` flow against temporary state dir.
   - Flask test client verifying web routes and decision flow.
   - CLI dispatch verification.
   - Backup/audit/journal artifacts and rollback behavior.

## Test Matrix

The table below maps test files to the areas they cover. For the current number of tests, see the output of `pytest`.

| Test File | Modules / Behaviour Covered |
|-----------|-----------------------------|
| `test_config_and_domain.py` | `config`, `domain/models` (config defaults, domain model creation, enum values) |
| `test_state_repository.py` | `state/repository` (JSON/JSONL persistence; Proposal roundtrip; load_decisions / load_skipped_ids / load_audit_log skip corrupt or invalid lines) |
| `test_gnucash_loader.py` | `gnucash/loader` (XML load/write, candidate filtering by Unspecified/Imbalance-GBP, backup) |
| `test_email_pipeline.py` | `email/parser` (body filtering, parse raises when file missing), `email/index` (build_or_load skips corrupt index lines and unparseable `.eml`) |
| `test_receipt_pipeline.py` | `receipt/ocr`, `receipt/repository` |
| `test_matching.py` | `matching/email_matcher` (incl. tolerance-zero, no-amount/empty-amount exclusion), `matching/receipt_matcher` |
| `test_prompts.py` | `prompts` (prompt directory selection, template loading precedence, placeholder rendering) |
| `test_ml_predictor.py` | `ml/predictor` (CategoryPredictor; §6.3 LLM: `_run_llm_flow` no paths / step1 None, no emails, extraction block; `run_llm_check` returns None on flow fail; `propose` `skip_llm`; `_query_llm` → `LlmSuggestion`; `get_emails_for_display`; subcategory formatting; extraction dict-only; `FeedbackTrainer`) |
| `test_llm_flow_cross_product.py` | `ml/predictor` LLM cross-product behaviour (main on/off, extraction on/off, `use_web` on/off, has_emails yes/no) |
| `test_pipeline.py` | `services/pipeline` (account_paths metadata, `run_config` LLM/extraction persistence, LLM connection test, email index min_date; behaviour when GnuCash has no `<gnc:book>` element) |
| `test_apply_engine.py` | `apply/engine` (dry-run incl. no decisions, apply, journal, rollback, audit; apply raises when no `run_config` / invalid GnuCash; rollback raises when no backup dir / no backup files; receipt not moved on amount mismatch) |
| `test_review.py` | `review/service` (get_account_paths, `run_llm_check` preserves ML and returns None when predictor fails; `submit_decision` saves unenriched when enrichment raises), `review/webapp` (review page, ML/LLM sections, `llm-check` 404/400/200; review 200 with empty emails when display build raises; `llm-check` 200 with `extraction: null` when serialisation fails; decide 400 for invalid action, 503 when save fails) |
| `test_integration.py` | Full run → review → apply cycle, Flask flow, CLI dispatch |
| `test_cli.py`, `test_cli_flags.py` | `cli` (parser, flags, LLM args; `--llm-use-web`; run/review/apply flags; dispatch integration tests) |
| `test_new_features.py` | New behaviours: recursive emails, new categories, evidence enrichment, terse items, evidence approval, no-evidence, refund detection, schema versions, enrichment wiring, corner cases |
| `test_system.py` | Full system tests: pipeline with multi-subdir emails, review cycle, apply+rollback, Flask webapp |
| `test_system_scenarios.py` | Scenarios: imbalance-only, zero candidates, date window, multiple receipts, nested emails, empty book, realistic format |

## Spec coverage (key requirements)

The test suite exercises all functional requirements from the main specification:

- **§3 Domain rules**: Target tx (Unspecified/Imbalance-GBP at any path segment), GBP-only, future excluded, skip persist/reload — `test_gnucash_loader`, `test_apply_engine`, `test_pipeline`, `test_review`.
- **§6–7 Components & evidence**: GnuCash load/write, candidate filter, email parse/index (min_date from earliest Unspecified/Imbalance-GBP tx), receipt OCR/match, ML prediction, review UI (queue, prev/next, evidence), apply (dry-run, backup, audit, journal, rollback), receipt moved only when amount-compatible — `test_gnucash_loader`, `test_email_pipeline`, `test_receipt_pipeline`, `test_matching`, `test_ml_predictor`, `test_pipeline`, `test_review`, `test_apply_engine`, `test_new_features`.
- **§6.3 LLM interaction architecture**: Entry points (run skip_llm, review "Check with LLM"); single flow (_run_llm_flow: extraction optional, step1/step2, None on failure); run_llm_check returns None when flow fails (API 400, proposal not updated); config from run_config; extraction serialization in API — `test_ml_predictor`, `test_review`, `test_pipeline`.
- **§9–10 Review & write**: One-by-one review, before/after, approve/edit/skip, no write without approval, backup/rollback — `test_review`, `test_apply_engine`, `test_integration`.
- **Error handling / graceful degradation**: State repository skips corrupt JSONL lines (decisions, skipped IDs, audit log); GnuCash loader get_tree/get_account_path defaults; email index build skips corrupt index lines and unparseable .eml; review enrichment failure non-fatal (decision saved); review page and llm-check handle display/serialization errors without 5xx — `test_state_repository`, `test_gnucash_loader`, `test_email_pipeline`, `test_review`.
- **§15 Acceptance criteria**: All 14 criteria are covered by the above plus `test_integration`, `test_system`, `test_system_scenarios`, `test_cli_flags`.

## Fixtures

- **`tests/conftest.py`**: `SAMPLE_GNUCASH_XML` string constant, `sample_gnucash_path` (gzip), `sample_gnucash_xml_path` (plain XML)
- **`tests/fixtures/emails/`**: 13 synthetic `.eml` files across 4 subdirectories (`bank_online_finance`, `amazon`, `subscriptions`, `utilities`) plus root-level files, modelled on Proton Mail export format
- **Receipt images**: Generated programmatically in test setup using Pillow `ImageDraw`

## Tooling
- Test runner: `pytest >= 8.0`
- Python support: CPython 3.11+
- Optional future plugins:
  - `pytest-cov` for coverage gates
  - `pytest-xdist` for parallelization
  - `hypothesis` for property-based matching/parser tests

## Conventions
- Test file naming: `tests/test_<module>.py`
- Test function naming: `test_<behavior>`
- Use AAA structure (Arrange/Act/Assert).
- Shared fixtures in `tests/conftest.py`.
- Use `tmp_path` for all I/O tests.

## Future Test Enhancements
- Add coverage reporting via `pytest-cov`.
- Add CI job to run `pytest` on every PR.
- Add larger synthetic datasets for performance/regression tests.
- Add `hypothesis` property-based tests for matching scoring edge cases.
- Add sample real-world degraded receipt images for OCR robustness testing.
