# GnuCash Enrichment Implementation Architecture

This document previously duplicated information that now lives in:

- The **main specification**: `docs/gnucash_email_receipt_categorization_spec.mdc`  
  (domain rules, inputs/outputs, component architecture, LLM interaction, acceptance criteria).
- The **Developers Guide**: `docs/developers_guide.mdc`  
  (priorities, how to update the spec and tests, detailed codebase structure, external dependencies).
- The **Test Plan & Strategy**: `docs/test_plan_and_strategy.mdc`  
  (testing levels, test matrix, spec coverage, fixtures).

To avoid divergence and duplicated maintenance, this file is kept as a lightweight pointer only.
For up-to-date architecture and module information, always consult the specification and
Developers Guide; for current test coverage, run `pytest` and see the test plan.
